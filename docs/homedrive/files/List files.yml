info:
  name: List files
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/files"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
