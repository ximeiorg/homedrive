info:
  name: file_sync
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/files/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
