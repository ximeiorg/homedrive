info:
  name: getUser
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/members/1"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
