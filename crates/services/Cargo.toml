[package]
name = "services"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
store = { path = "../store" }
schema = { path = "../schema" }
serde_json = "1.0"
thiserror = "2.0.18"
sea-orm = { version = "2.0.0-rc", features = ["runtime-tokio-rustls", "sqlx-sqlite"] }
axum = { version = "*" }
hyper = { version = "*" }
tracing = { version = "*" }