[package]
name = "schema"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = {workspace = true }
chrono = { version = "0.4", features = ["serde"] }
validator = { version = "0.20.0", features = ["derive"] }
